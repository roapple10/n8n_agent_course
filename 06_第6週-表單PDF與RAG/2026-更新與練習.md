# 2026 更新：模組化、RAG 與穩定性

進階不等於堆更多節點。把可重用步驟拆成子工作流，將環境差異放進 credentials 或 variables，對寫入動作設計唯一鍵或 idempotency。RAG 則要分離文件載入、切塊、嵌入、向量儲存、檢索、回答與評估；回答必須回傳來源，無法檢索時明確拒答。Error Trigger 與告警流程需獨立於主流程，避免錯誤處理本身被錯誤中斷。

## 課堂練習

使用 `06-文件處理與人工作業.json` 設計一個可暫停的覆核流程。再以 `07-RAG-檢索與評估.json` 畫出 RAG 所需節點，列出十個問題與至少兩個應該拒答的問題。
