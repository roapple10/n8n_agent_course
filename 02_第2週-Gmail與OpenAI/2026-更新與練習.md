# 2026 更新：工作流是可測試的資料產品

把 n8n 視為整合與編排平台，而不是單純的「串接工具」。一個可靠流程有明確的資料契約、可觀察的執行紀錄、最小權限的憑證，以及重跑後不重複造成外部副作用的設計。AI 是不確定的處理器，適合摘要、抽取與分類；高風險動作仍應由規則、驗證和人工覆核把關。

## 課堂練習

挑一個手動工作：例如整理請假信。寫出 trigger、輸入欄位、處理規則、輸出位置與失敗處理。再以 `02-HTTP-API-資料清洗.json` 驗證「只留下必要欄位」的原則。
